<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="nagasawakenji.WalkFind.infra.MyBatis.mapper.UserMapper">

    <resultMap id="UserResultMap" type="nagasawakenji.WalkFind.domain.model.User">
        <id property="id" column="id"/>
    </resultMap>

    <select id="findById" resultMap="UserResultMap">
        SELECT id, username, email, role, is_active, created_at, updated_at
        FROM users
        WHERE id = #{userId}
    </select>

    <insert id="insert">
        INSERT INTO users (id, username, email, role)
        VALUES (#{id}, #{username}, #{email}, #{role})
    </insert>

    <update id="updateUsername">
        UPDATE users
        SET username = #{username}, updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

</mapper>